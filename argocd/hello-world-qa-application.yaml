apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-qa
  namespace: argocd
  labels:
    app: hello-world
    environment: qa
    essesseff: "true"
    config-env-repo-id: "{{REPOSITORY_ID}}"
spec:
  project: default
  source:
    repoURL: https://github.com/essesseff-hello-world-go-template/hello-world-config-qa
    targetRevision: main
    path: .
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: essesseff-hello-world-go-template
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
  info:
    - name: hello-world
      value: "hello-world GitOps deployment managed by essesseff"
    - name: environment
      value: "qa"

